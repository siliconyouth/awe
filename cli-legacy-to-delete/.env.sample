# AWE CLI Environment Configuration
# This file will be encrypted by dotenvx for enhanced security
# Copy this file to .env and fill in your values

# ==============================================
# 🔐 SUPABASE CONFIGURATION (Required for Cloud Features)
# ==============================================
# Get these from: https://app.supabase.com/project/_/settings/api

# Your Supabase project URL
AWE_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase anonymous key (safe for client-side use)
AWE_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase service role key (KEEP SECRET - server-side only)
# Optional: Only needed for advanced admin features
AWE_SUPABASE_SERVICE_KEY=your-service-role-key-here

# ==============================================
# ⚡ PERFORMANCE CONFIGURATION (Optional)
# ==============================================

# Memory cache size (number of entries)
AWE_CACHE_SIZE=1000

# Maximum concurrent API requests
AWE_MAX_CONCURRENCY=10

# Force offline mode (disables cloud features)
AWE_OFFLINE_MODE=false

# API request timeout in milliseconds
AWE_API_TIMEOUT=30000

# Number of retry attempts for failed requests
AWE_API_RETRIES=3

# ==============================================
# 🎛️ FEATURE FLAGS (Optional)
# ==============================================

# Comma-separated list of enabled features
AWE_FEATURES=aiAnalysis,templateGeneration,backgroundSync,vectorSearch

# ==============================================
# 📊 PRIVACY & TELEMETRY (Optional)
# ==============================================

# Enable anonymous usage analytics (helps improve AWE)
AWE_TELEMETRY_ENABLED=false

# Enable crash reporting
AWE_CRASH_REPORTING=false

# ==============================================
# 🔧 DEVELOPMENT OPTIONS (Optional)
# ==============================================

# Enable debug logging
AWE_DEBUG=false

# Custom API base URL (for development)
# AWE_API_BASE_URL=https://api.awe-cli.com

# Log level: error, warn, info, debug
AWE_LOG_LEVEL=info